# tools/docs — Sphinx documentation.

set shell := ["bash", "-c"]

# Build the HTML docs
build:
    uv run --group docs sphinx-build -b html docs docs/_build/html

# Serve the built docs locally
serve: build
    @python3 -m http.server -d docs/_build/html 8000
